Download the .rar version & just open with VS Code or Set up your local server using XAMMP

The "Application" Folder contains all the files before PHP / PHPMYADMIN was implemented (Meaning it's just all the normal files before we hooked it up to the database).
